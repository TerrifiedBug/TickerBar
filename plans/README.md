# Implementation Plans

Generated by the improve skill on 2026-06-17 (against commit `c0c912e`). Each plan
is self-contained — read it fully before starting, honor its STOP conditions, and
update your status row when done. Execute in the order below unless a plan's
"Depends on" says otherwise.

Build: `xcodebuild -project TickerBar.xcodeproj -scheme TickerBar -configuration Release build`
Test: `xcodebuild -project TickerBar.xcodeproj -scheme TickerBar -destination 'platform=macOS' test`

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add CI (build+test on PR/push) + shared scheme | P1 | S | — | DONE |
| 002 | Add MIT LICENSE file | P1 | S | — | DONE |
| 004 | Resolve project.yml drift (delete stale spec) | P1 | S | — | DONE |
| 003 | Fix stale README (market-hours toggle, build path) | P2 | S | — | DONE |
| 008 | Inject UserDefaults into StockService | P1 | M | — | DONE |
| 005 | Build Yahoo URLs with URLComponents (encoding) | P1 | M | — | DONE |
| 006 | Surface missing-FX-rate instead of silent 1.0 | P1 | M | — | DONE |
| 007 | Consolidate triplicated sub-unit currency logic | P2 | S | — | DONE |
| 019 | Multi-lot holdings (RSU value-only + purchases) | P2 | L | 008, 006 | DONE |
| 009 | Unit tests: money/alert/market-hours | P1 | M | 008, (006/007/019) | DONE |
| 011 | Fix currentDisplayStock/index desync | P3 | S | 009 | DONE |
| 010 | Improve market-hours accuracy (lunch/marketState) | P2 | M | 009 | DONE |
| 012 | Reduce N+1 fetch (v7 primary, v8 sparkline-only) | P2 | M | 009, 005 | DEFERRED — STOP condition: needs live-Yahoo verification that v7 returns previousClose/day-range/timezone for all symbol types, and would drop per-row sparklines. Not safe to land without runtime verification (no network in this env). |
| 016 | Surface 52w/pre-post/day-range inline | P2 | M | 009 | DONE |
| 015 | Export & import watchlist/holdings/alerts | P2 | M | 019 | DONE |
| 017 | Percent-change & recurring alerts | P2 | M | 009 | DONE |
| 014 | Split StockService god object | P3 | L | 009, 005, 006, 007, 008 | PARTIAL — pure `PortfolioCalculator` extracted + unit-tested (the high-value, low-risk seam). The wider `YahooFinanceClient`/`SettingsStore` extraction is deferred: the verifier downgraded its value (pure pieces already factored + tested) and it's a high-risk Swift-6 actor-isolation refactor for maintainability only. |
| 013 | Developer ID signing + notarization | P1 | M | — (needs Apple cert) | DONE (code) — release.yml now signs with Developer ID + notarizes when these secrets are set: DEVELOPER_ID_CERT_P12_BASE64, DEVELOPER_ID_CERT_PASSWORD, DEVELOPER_ID_IDENTITY, NOTARY_APPLE_ID, NOTARY_PASSWORD, NOTARY_TEAM_ID; falls back to ad-hoc otherwise (no regression). ACTIVATION needs a paid Apple Developer ID cert + those secrets, and validation on a real tagged release. |
| 018 | WidgetKit widget (spike) | P3 | L | 008 | DEFERRED — spike requires adding a new app-extension target + App Group entitlement, which needs Xcode (not safe via hand-edited pbxproj here). Groundwork is ready: fetch/parse are `nonisolated static`, `StockItem` is `Codable`. |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **001 first** — it's the verification backbone; every later plan's "Test" gate is only enforced once CI runs on PRs.
- **008 before 009/019** — injecting UserDefaults is the seam that lets tests run against an isolated store instead of polluting real prefs.
- **006 + 019 before 009's portfolio tests** — 006 changes the FX-missing contract and 019 changes the holdings model; writing the portfolio/FX tests against the final shape avoids rewriting them.
- **019 before 015** — export/import must serialize the multi-lot holdings shape.
- **009 before 010/011/012/017** — characterization/unit tests pin current behavior before these change fetch paths, rotation, market-hours, or the alert model.
- **014 last among code plans** — the god-object split is a wide refactor; do the smaller fixes (005/006/007/008/019) and land 009's tests first so the refactor is verified.

## Findings considered and rejected (do not re-audit)

- **Crumb cached for process lifetime, no expiry**: self-heals — `invalidateAuth()` clears the crumb on any full-empty fetch result, so the next cycle re-auths. Only a minor proactive-expiry nicety.
- **Weak crumb content validation**: a `statusCode == 200` gate already exists; the crumb is an opaque TLS-delivered token. Over-validating risks breaking when Yahoo changes format.
- **Sparkle key via process-substitution `<(echo ...)`**: standard and safe (bash builtin, anonymous /dev/fd pipe). The proposed `mktemp` "fix" would regress by writing the key to disk.
- **Menu-bar NSImage re-render every body eval**: under `@Observable`, body re-evaluates only on real changes (rotation tick ~5s, price update); one tiny image alloc is negligible.
- **Sparkle `upToNextMajorVersion` floor of 2.5.0**: `Package.resolved` pins 2.8.1 exactly — range-in-manifest + exact-in-lockfile is the standard SPM convention.
- **Market-hours doc/heuristic reconciliation (docs)**: the heuristic is self-labeled "Approximate" with an upgrade note already recorded; only the lunch-break omission is non-obvious (covered by plan 010, not a separate docs plan).
- **Per-render perf micro-opts** (Calendar/TimeZone alloc in `isMarketOpen`; 3× portfolio reduce per body; currency-symbol dedup): real but imperceptible on a near-idle menu-bar app with a handful of symbols. The only perf item worth doing is the N+1 network fetch (plan 012). The currency-symbol DRY cleanup folds into plan 007.

## What was not audited

Couldn't run the app or hit live Yahoo (no dynamic analysis). `docs/` is gitignored, so the local `docs/yahoo-finance-apis.md` is excluded from committed plans. Sparkle's vendored code and the app icon assets were not reviewed.
